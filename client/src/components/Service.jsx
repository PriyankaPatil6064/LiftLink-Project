import React from "react";

// Service detail page — placeholder for future implementation
// Could show detailed info about a specific service type (Elevators, Escalators, etc.)
const Service = () => {
  return (
    <div
      style={{
        minHeight: "calc(100vh - 56px)",
        background: "#0A192F",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        flexDirection: "column",
        gap: "1rem",
      }}
    >
      <h2 style={{ color: "#f8e3a1" }}>Service Details</h2>
      <p style={{ color: "rgba(255,255,255,0.5)" }}>Coming soon.</p>
    </div>
  );
};

export default Service;
